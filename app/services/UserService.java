package services;

import models.User;

/**
 * User's service.
 *
 * @author vincent
 *
 */
public class UserService {

    /**
     * Load one user.
     *
     * @param user User to search.
     * @return If not find user return null, otherwise user with data.
     */
    public User load(final User user) {

        return user;
    }
}
